namespace Geosnap.Result
{
    /// <summary>
    /// Represents no meaningful result.  Used to create a result that only carries failure information (Result<Nothing>).
    /// </summary>
    public class Nothing { }
}
